#include "main.h"
/**
 *mul - function that multiplies two numbers ( a and b)
 *@a: 1st number to be multiplied
 *@b: 2nd number to be multiplied
 *Return: the product of 'a' and 'b'
 */

int mul(int a, int b)
{
return (a * b);
}
