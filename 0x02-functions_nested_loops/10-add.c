#include "main.h"
/**
*add - add two int numbers and return the value
*@a : - the first number
*@b : - The second number to add with the add function
* Return: void
*/
int add(int a, int b)
{
int result;

result = a + b;
return (result);
}
